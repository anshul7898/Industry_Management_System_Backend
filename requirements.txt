# Web framework - using stable version with proven Pydantic v2 support
fastapi==0.104.1

# AWS Lambda adapter for FastAPI/ASGI
mangum>=0.21.0

# AWS SDK
boto3>=1.34.0
botocore>=1.34.0

# Data validation - Pydantic v2 stable
pydantic==2.5.0

# Environment variable loading
python-dotenv>=1.0.0

# Local development server (not needed in Lambda, only for local runs)
# Use plain uvicorn without C extensions to avoid cross-platform issues
uvicorn>=0.29.0
